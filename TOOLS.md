# maltego-mcp — Tool Reference

Auto-generated by `scripts/gen_tools_doc.py` from the live MCP tool schemas. **49 tools.**

Every tool takes its arguments inside a single `params` object. Arguments marked **required** must be supplied; others have defaults.

## `maltego_add_entity`

Add an entity (node) to the active graph.

| Argument | Type | Required | Default | Description |
|----------|------|----------|---------|-------------|
| `type` | string | yes |  | Maltego entity type id, e.g. 'maltego.Domain', 'maltego.IPv4Address', 'maltego.Person'. Use maltego_list_entity_types to discover options. |
| `value` | string | yes |  | Primary value of the entity, e.g. 'example.com' or '8.8.8.8'. |
| `properties` | object / null | no |  | Optional extra Maltego properties as a name->value map. |
| `notes` | string / null | no |  | Optional free-text notes for the entity. |
| `dedupe` | boolean | no | `True` | If true (default), reuse an existing entity with the same type+value instead of creating a duplicate. |

## `maltego_add_link`

Add a directed link (edge) between two existing entities on the active graph.

| Argument | Type | Required | Default | Description |
|----------|------|----------|---------|-------------|
| `source_id` | string | yes |  | Id of the source entity (e.g. 'n0'). |
| `target_id` | string | yes |  | Id of the target entity (e.g. 'n1'). |
| `label` | string / null | no | `` | Optional label drawn on the link, e.g. 'resolves to'. |

## `maltego_apply_layout`

Compute and assign (x, y) layout positions to entities on the active graph.

| Argument | Type | Required | Default | Description |
|----------|------|----------|---------|-------------|
| `algorithm` | string | no | `hierarchical` | Layout algorithm: 'hierarchical', 'radial', or 'force'. |

## `maltego_create_graph`

Create a new, empty in-memory Maltego graph and make it active.

| Argument | Type | Required | Default | Description |
|----------|------|----------|---------|-------------|
| `name` | string | yes |  | Name for the new graph (e.g. 'acme-investigation'). |

## `maltego_delete_entity`

Delete an entity and all links touching it from the active graph.

| Argument | Type | Required | Default | Description |
|----------|------|----------|---------|-------------|
| `entity_id` | string | yes |  | Id of the entity to delete (its links are removed too). |

## `maltego_delete_graph`

Remove an open graph from the server's memory.

| Argument | Type | Required | Default | Description |
|----------|------|----------|---------|-------------|
| `name` | string | yes |  | Name of the open graph to remove from the server. |

## `maltego_delete_link`

Delete a single link (edge) from the active graph by id.

| Argument | Type | Required | Default | Description |
|----------|------|----------|---------|-------------|
| `link_id` | string | yes |  | Id of the link to delete (e.g. 'e0'). |

## `maltego_explain_entity`

Explain one entity: its data, neighbours, and how it can be expanded.

| Argument | Type | Required | Default | Description |
|----------|------|----------|---------|-------------|
| `entity_id` | string | yes |  | Id of the entity to explain (e.g. 'n0'). |
| `response_format` | object | no | `markdown` | Output format: 'markdown' or 'json'. |

## `maltego_explain_scores`

Explain how an entity's intelligence-quality scores were derived.

| Argument | Type | Required | Default | Description |
|----------|------|----------|---------|-------------|
| `entity_id` | string | yes |  | Entity id to score (e.g. 'n0'). |

## `maltego_explain_transform`

Explain one recorded transform execution by its execution id.

| Argument | Type | Required | Default | Description |
|----------|------|----------|---------|-------------|
| `transform_execution_id` | string | yes |  | Execution id of a recorded step (e.g. 'x0'). See maltego_list_investigation_steps. |
| `response_format` | object | no | `markdown` | Output format: 'markdown' or 'json'. |

## `maltego_explain_why`

Explain why an entity is on the graph: which step/transform discovered it.

| Argument | Type | Required | Default | Description |
|----------|------|----------|---------|-------------|
| `entity_id` | string | yes |  | Entity id to explain the provenance of (e.g. 'n3'). |
| `response_format` | object | no | `markdown` | Output format: 'markdown' or 'json'. |

## `maltego_export_csv`

Export the active graph's entities to a CSV file (round-trips with import).

| Argument | Type | Required | Default | Description |
|----------|------|----------|---------|-------------|
| `path` | string | yes |  | Destination file path for the export. |

## `maltego_export_json`

Export the full active graph (entities, links, memory, scores) as JSON.

| Argument | Type | Required | Default | Description |
|----------|------|----------|---------|-------------|
| `path` | string | yes |  | Destination file path for the export. |

## `maltego_export_report`

Write a deterministic investigation report for the active graph to a file.

| Argument | Type | Required | Default | Description |
|----------|------|----------|---------|-------------|
| `path` | string | yes |  | Destination file path for the report. |
| `format` | object | no | `markdown` | Report format: 'markdown' or 'html'. |

## `maltego_generate_report`

Generate a deterministic investigation report for the active graph.

| Argument | Type | Required | Default | Description |
|----------|------|----------|---------|-------------|
| `format` | object | no | `markdown` | Report format: 'markdown' or 'html'. |

## `maltego_get_entity`

Fetch full details of one entity on the active graph by id.

| Argument | Type | Required | Default | Description |
|----------|------|----------|---------|-------------|
| `entity_id` | string | yes |  | Id of the entity to fetch (e.g. 'n0'). |
| `response_format` | object | no | `markdown` | Output format: 'markdown' or 'json'. |

## `maltego_get_investigation_timeline`

Return the chronological timeline of the active investigation.

_No arguments._

## `maltego_get_recent_events`

Return recent investigation events (entity_discovered, transform_*, etc.).

| Argument | Type | Required | Default | Description |
|----------|------|----------|---------|-------------|
| `limit` | integer | no | `50` | Maximum recent events to return. |
| `since_seq` | integer / null | no |  | Only return events with sequence number greater than this. |
| `response_format` | object | no | `json` | Output format: 'json' (default) or 'markdown'. |

## `maltego_identify_pivots`

Identify the most promising pivot entities in the active graph.

| Argument | Type | Required | Default | Description |
|----------|------|----------|---------|-------------|
| `limit` | integer | no | `10` | Maximum items to return. |
| `response_format` | object | no | `markdown` | Output format: 'markdown' or 'json'. |

## `maltego_import_csv`

Build entities (and optional links) on the active graph from CSV.

| Argument | Type | Required | Default | Description |
|----------|------|----------|---------|-------------|
| `path` | string / null | no |  | Path to a CSV file with 'type,value' columns (optionally 'notes','link_to'). |
| `content` | string / null | no |  | Raw CSV text (alternative to 'path'). |

## `maltego_import_graph`

Merge a .mtgx file's contents into the ACTIVE graph (combine investigations).

| Argument | Type | Required | Default | Description |
|----------|------|----------|---------|-------------|
| `path` | string | yes |  | Path to a .mtgx file whose entities/links are merged into the active graph. |
| `dedupe` | boolean | no | `True` | Merge entities matching an existing (type, value) instead of duplicating them. |

## `maltego_investigate`

One-call investigation: detect, build, expand, analyze, and recommend.

| Argument | Type | Required | Default | Description |
|----------|------|----------|---------|-------------|
| `query` | string | yes |  | Anything to investigate: a domain, email, IPv4/IPv6, or URL. The type is auto-detected. |
| `allow_network` | boolean | no | `True` | Run network transforms (default True). Set false for offline/passive. |
| `max_rounds` | integer / null | no |  | Override the chosen machine's expansion depth. |
| `layout` | string | no | `hierarchical` | Layout to apply after investigating: 'hierarchical', 'radial', or 'force'. |

## `maltego_investigate_domain`

Automatically investigate a domain by adding it and running transforms.

| Argument | Type | Required | Default | Description |
|----------|------|----------|---------|-------------|
| `value` | string | yes |  | The seed value to investigate (domain, email, or IP). |
| `allow_network` | boolean | no | `True` | Run transforms that make network calls (DNS, OSINT APIs). Set false for an offline/passive parse-only run. |
| `max_rounds` | integer | no | `2` | How many expansion rounds to run (breadth-first). Higher = deeper, more tool calls. |

## `maltego_investigate_email`

Automatically investigate an email address (domain, breaches, footprint).

| Argument | Type | Required | Default | Description |
|----------|------|----------|---------|-------------|
| `value` | string | yes |  | The seed value to investigate (domain, email, or IP). |
| `allow_network` | boolean | no | `True` | Run transforms that make network calls (DNS, OSINT APIs). Set false for an offline/passive parse-only run. |
| `max_rounds` | integer | no | `2` | How many expansion rounds to run (breadth-first). Higher = deeper, more tool calls. |

## `maltego_investigate_ip`

Automatically investigate an IPv4 address (reverse DNS, ports, services).

| Argument | Type | Required | Default | Description |
|----------|------|----------|---------|-------------|
| `value` | string | yes |  | The seed value to investigate (domain, email, or IP). |
| `allow_network` | boolean | no | `True` | Run transforms that make network calls (DNS, OSINT APIs). Set false for an offline/passive parse-only run. |
| `max_rounds` | integer | no | `2` | How many expansion rounds to run (breadth-first). Higher = deeper, more tool calls. |

## `maltego_learning_stats`

Show cross-investigation learning stats used by the Next Best Action engine.

_No arguments._

## `maltego_list_entities`

List entities on the active graph, with optional filtering and pagination.

| Argument | Type | Required | Default | Description |
|----------|------|----------|---------|-------------|
| `type_filter` | string / null | no |  | Only return entities of this Maltego type (e.g. 'maltego.IPv4Address'). |
| `value_contains` | string / null | no |  | Only return entities whose value contains this substring (case-insensitive). |
| `limit` | integer | no | `50` | Maximum entities to return. |
| `offset` | integer | no | `0` | Number of entities to skip (pagination). |
| `response_format` | object | no | `markdown` | Output format: 'markdown' or 'json'. |

## `maltego_list_entity_types`

List supported Maltego entity types from the built-in catalog.

| Argument | Type | Required | Default | Description |
|----------|------|----------|---------|-------------|
| `category` | string / null | no |  | Filter to one category (e.g. 'infrastructure', 'personal', 'social'). |

## `maltego_list_graphs`

List all graphs currently open in the server and mark the active one.

_No arguments._

## `maltego_list_investigation_steps`

List recorded investigation steps (procedural memory) for the active graph.

| Argument | Type | Required | Default | Description |
|----------|------|----------|---------|-------------|
| `limit` | integer | no | `50` | Maximum steps to return. |
| `offset` | integer | no | `0` | Steps to skip (pagination). |
| `status` | string / null | no |  | Filter by status: 'success', 'empty', or 'error'. |
| `response_format` | object | no | `markdown` | Output format: 'markdown' or 'json'. |

## `maltego_list_links`

List links (edges) on the active graph, with pagination.

| Argument | Type | Required | Default | Description |
|----------|------|----------|---------|-------------|
| `limit` | integer | no | `50` | Maximum links to return. |
| `offset` | integer | no | `0` | Links to skip (pagination). |
| `response_format` | object | no | `markdown` | Output format: 'markdown' or 'json'. |

## `maltego_list_machines`

List available investigation machines (reusable workflow templates).

_No arguments._

## `maltego_list_providers`

List OSINT transform providers and whether each is configured.

_No arguments._

## `maltego_list_transforms`

List available transforms, optionally filtered by accepted input type.

| Argument | Type | Required | Default | Description |
|----------|------|----------|---------|-------------|
| `input_type` | string / null | no |  | Only list transforms that accept this Maltego entity type as input. |

## `maltego_load_graph`

Load an existing .mtgx file as a new active graph (continue an investigation).

| Argument | Type | Required | Default | Description |
|----------|------|----------|---------|-------------|
| `path` | string | yes |  | Path to an existing .mtgx file to load. |

## `maltego_next_best_actions`

Recommend the most valuable next investigative moves (decision engine).

| Argument | Type | Required | Default | Description |
|----------|------|----------|---------|-------------|
| `limit` | integer | no | `10` | Maximum items to return. |
| `response_format` | object | no | `markdown` | Output format: 'markdown' or 'json'. |

## `maltego_open_graph`

Open an existing Maltego ``.mtgx`` file into memory and make it active.

| Argument | Type | Required | Default | Description |
|----------|------|----------|---------|-------------|
| `path` | string | yes |  | Absolute or relative path to an existing .mtgx file. |

## `maltego_rank_entities`

Rank entities by investigation priority (deterministic).

| Argument | Type | Required | Default | Description |
|----------|------|----------|---------|-------------|
| `limit` | integer | no | `20` | Maximum entities to return. |
| `response_format` | object | no | `markdown` | Output format: 'markdown' or 'json'. |

## `maltego_rename_graph`

Rename an open graph (defaults to the active graph).

| Argument | Type | Required | Default | Description |
|----------|------|----------|---------|-------------|
| `new_name` | string | yes |  | New name for the graph. |
| `graph_name` | string / null | no |  | Graph to rename. Defaults to the active graph. |

## `maltego_reset_learning`

Clear the cross-investigation learning store (in-memory and on disk).

_No arguments._

## `maltego_run_machine`

Run an investigation machine against a seed value on the active graph.

| Argument | Type | Required | Default | Description |
|----------|------|----------|---------|-------------|
| `machine_name` | string | yes |  | Machine to run, e.g. 'passive_domain'. Use maltego_list_machines. |
| `seed_value` | string | yes |  | Seed value for the machine (e.g. the domain or email to investigate). |
| `allow_network` | boolean / null | no |  | Override the machine's network setting (default: use the machine's own). |
| `max_rounds` | integer / null | no |  | Override the machine's expansion depth. |

## `maltego_run_transform`

Run a transform on an entity in the active graph to discover related entities.

| Argument | Type | Required | Default | Description |
|----------|------|----------|---------|-------------|
| `transform_name` | string | yes |  | Name of the transform to run, e.g. 'dns.domain_to_ip'. Use maltego_list_transforms to discover. |
| `entity_id` | string | yes |  | Id of the input entity in the active graph to run the transform on. |
| `add_to_graph` | boolean | no | `True` | If true (default), add resulting entities and links to the active graph. |

## `maltego_save_graph`

Save a graph to a Maltego ``.mtgx`` file that opens in Maltego CE.

| Argument | Type | Required | Default | Description |
|----------|------|----------|---------|-------------|
| `path` | string / null | no |  | Destination .mtgx path. If omitted, re-saves to the path the graph was opened from. |
| `graph_name` | string / null | no |  | Name of the graph to save. Defaults to the active graph. |

## `maltego_score_entity`

Compute intelligence-quality scores for one entity (deterministic).

| Argument | Type | Required | Default | Description |
|----------|------|----------|---------|-------------|
| `entity_id` | string | yes |  | Entity id to score (e.g. 'n0'). |

## `maltego_set_active_graph`

Make a previously-opened graph the active target for entity/link operations.

| Argument | Type | Required | Default | Description |
|----------|------|----------|---------|-------------|
| `name` | string | yes |  | Name of an already-open graph to make active. |

## `maltego_subscribe_events`

Enable real-time investigation mode and return a subscription id.

_No arguments._

## `maltego_suggest_next_steps`

Suggest concrete next transforms to run on the active graph.

| Argument | Type | Required | Default | Description |
|----------|------|----------|---------|-------------|
| `limit` | integer | no | `10` | Maximum items to return. |
| `response_format` | object | no | `markdown` | Output format: 'markdown' or 'json'. |

## `maltego_summarize_graph`

Summarize the active investigation graph (composition + key entities).

| Argument | Type | Required | Default | Description |
|----------|------|----------|---------|-------------|
| `response_format` | object | no | `markdown` | Output format: 'markdown' or 'json'. |

## `maltego_update_entity`

Update an entity's value, properties, notes, or weight on the active graph.

| Argument | Type | Required | Default | Description |
|----------|------|----------|---------|-------------|
| `entity_id` | string | yes |  | Id of the entity to update. |
| `value` | string / null | no |  | New primary value. |
| `properties` | object / null | no |  | Properties to set/merge (name->value). |
| `notes` | string / null | no |  | Replacement notes text. |
| `weight` | integer / null | no |  | Node weight / relevance. |
